export interface Player {
  readonly _id: string;
  readonly phone: string;
  readonly email: string;
  name: string;
  ranking: string;
  rankPosition: number;
  photoUrl: string;
  category: string;
}
